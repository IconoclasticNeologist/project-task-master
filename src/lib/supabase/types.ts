// Placeholder until generated from the live schema:  bun run gen:types
// (runs `supabase gen types typescript --linked`). Until then the client is untyped.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
