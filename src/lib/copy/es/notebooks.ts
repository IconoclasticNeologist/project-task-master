// Spanish notebooks — SCAFFOLD mirroring English until translated (same
// slugs and order forever; only the words change).
import { notebooks, type Notebook } from "../notebooks";

export const notebooksEs: readonly Notebook[] = notebooks;
